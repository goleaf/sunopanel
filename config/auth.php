<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Authentication Defaults
    |--------------------------------------------------------------------------
    |
    | This application does not use user authentication. These settings are
    | kept minimal to prevent Laravel from breaking.
    |
    */

    'defaults' => [
        'guard' => null,
        'passwords' => null,
    ],

    /*
    |--------------------------------------------------------------------------
    | Authentication Guards
    |--------------------------------------------------------------------------
    |
    | No authentication guards are configured as this application does not
    | use user authentication.
    |
    */

    'guards' => [
        // No guards configured
    ],

    /*
    |--------------------------------------------------------------------------
    | User Providers
    |--------------------------------------------------------------------------
    |
    | No user providers are configured as this application does not use
    | user authentication.
    |
    */

    'providers' => [
        // No providers configured
    ],

    /*
    |--------------------------------------------------------------------------
    | Resetting Passwords
    |--------------------------------------------------------------------------
    |
    | Password reset functionality is disabled as this application does not
    | use user authentication.
    |
    */

    'passwords' => [
        // No password reset configured
    ],

    /*
    |--------------------------------------------------------------------------
    | Password Confirmation Timeout
    |--------------------------------------------------------------------------
    |
    | Not applicable as this application does not use user authentication.
    |
    */

    'password_timeout' => 0,

];
